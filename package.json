{
  "name": "rec-corder",
  "private": true,
  "version": "0.2.1",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "sync-version": "node scripts/sync-version.js",
    "build:release": "npm run sync-version && npx tauri build",
    "clean": "cd src-tauri && cargo clean"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
