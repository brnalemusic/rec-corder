# Broad shell allowance for trusted Gemini workflows.
# Loaded explicitly via `policyPaths` in workflow settings.
[[rule]]
toolName = "run_shell_command"
decision = "allow"
priority = 999
allowRedirection = true
